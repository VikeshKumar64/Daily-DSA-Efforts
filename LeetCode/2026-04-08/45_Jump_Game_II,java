class Solution {
    public int jump(int[] nums) {
        int steps = 0;
        int cEnd = 0;
        int far = 0;

        for(int i = 0; i < nums.length-1; i++){
            far = Math.max(far, i + nums[i]);

            if(cEnd == i){
                steps++;
                cEnd = far;
            }
        }
        return steps;
    }
}